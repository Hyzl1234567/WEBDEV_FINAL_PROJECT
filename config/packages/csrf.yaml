# Stateless CSRF for Symfony Form components (UX Turbo compatible)
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        stateless_token_ids:
            - submit
            # 'authenticate' and 'logout' use session-based CSRF (no client JS required)
